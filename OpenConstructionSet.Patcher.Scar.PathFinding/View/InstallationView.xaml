<UserControl x:Class="OpenConstructionSet.Patcher.Scar.PathFinding.View.InstallationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpenConstructionSet.Patcher.Scar.PathFinding.View"
             xmlns:vm="clr-namespace:OpenConstructionSet.Patcher.Scar.PathFinding.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:DesignInstallationViewModel, IsDesignTimeCreatable=True}">
    <Grid>
        <StackPanel>
            <TextBlock>
                <Run Text="{Binding Name}" />
                <Run Text=" (" />
                <Run Text="{Binding Installation.Game}" />
                <Run Text=")" />
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
