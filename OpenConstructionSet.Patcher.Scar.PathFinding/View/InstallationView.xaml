<UserControl x:Class="OpenConstructionSet.Patcher.Scar.PathFinding.View.InstallationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ocs="clr-namespace:OpenConstructionSet.Installations;assembly=OpenConstructionSet"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=ocs:IInstallation, IsDesignTimeCreatable=False}">
    <Grid>
        <StackPanel>
            <TextBlock>
                <Run Text="{Binding Identifier, Mode=OneTime}" />
                <Run Text=" (" />
                <Run Text="{Binding Path, Mode=OneTime}" />
                <Run Text=")" />
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>